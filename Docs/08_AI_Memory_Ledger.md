# AI Memory Ledger

LocalForge must treat AI output as evidence, not truth.

Current implementation blocks unsafe command execution guidance and labels unimplemented modules as stubs instead of pretending they are complete.
