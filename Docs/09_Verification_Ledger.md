# Verification Ledger

Current verification:

- `swift test` with Xcode toolchain and project-local module cache.
- Core tests cover scanner snapshots, persistence round-trip, bookmark error states, command safety, redaction, scan policy defaults, and Guardian prioritization.
- Added (2026-06-07): project classifier (Swift package / Node / unidentified), AUv3 Info.plist detection, and Git non-repository handling. Suite now 10/10.
- Observed 2026-06-07: `swift test` 10/10; `./script/build_and_run.sh --verify` builds, bundles, launches, and confirms the process; CLI `scan` reports detected type + Git state; `assess-command "git reset --hard"` returns Blocked.
- Note: in the test sandbox the Git subprocess fails fast (Process is sandbox-restricted), so a successful Git path is verified via the CLI binary, not the unit tests.
- Phase 2 (2026-06-07): added applicability-by-kind and honest reality-scoring tests. Suite now 12/12. Verified the AUv3 plist now classifies specifically as an Effect (was generic Plugin).
- Observed 2026-06-07: `localforge scan` on this repo reports "SwiftUI App [Inferred]", reality 51%, top risk "No observed evidence that build works", next action "Run a build and confirm it succeeds" — confirming the command-centre output end to end. `build_and_run.sh --verify` exit 0.
- Phase 3 (2026-06-07): added verification-reconcile, reality-from-verification, and mission/verification persistence tests. Suite now 15/15. `build_and_run.sh --verify` exit 0.
- The reality score now distinguishes verification states: failures lower it and surface as the top risk + next action; full verified coverage with no failures is the only way to reach 100. Self-reported verification is user evidence (decays over time conceptually; `updatedAt` recorded).
- Phase 4 (2026-06-07): added setup-draft, verification-timeline, verifier metadata, and knowledge-note persistence tests. Suite now 17/17.
- Phase 5–6.5 (2026-06-08): PromptForge artefacts, command safety, journal capping, release-readiness blocking, dependency surfacing, cross-project ranking, Guardian enrichment. Suite grew 17 → 30.
- Phase 7 (2026-06-08): evidence backward-compat decode, evidence protects stale verified trust, open critical risks penalise Reality, active assumptions surface, template catalogue + dependency-wired packs, handoff includes all four registers. Suite 30 → 36.
- Phase 7.5 (2026-06-09): cross-link round-trip + old-record default-to-empty, reality breakdown attribution (+/− contributions), confidence-separate-from-reality, register-health coverage, **bidirectional related-records resolution from a single stored link**, verification bridges by UUID and by area name. Suite 36 → **42/42**. `./script/build_and_run.sh --verify` → built, bundled, launched, process confirmed, exit 0.
- Phase 8 (2026-06-09): **SQLite round-trip is value-equal** (full fat state incl. nil-vs-[] distinction and bookmark Data), **legacy UserDefaults migration imports and never deletes the blob**, **corruption recovery rebuilds from backup and keeps the damaged file beside it**, universal search across all ten record types with release-blocking flags, portfolio truth counts (critical open risks + journal), packs carry suggested risks + matured dependency chains. Suite 42 → **48/48**. Launch verify exit 0; `workspace.sqlite` (WAL) confirmed created in Application Support on launch.
- Honest observation (2026-06-09): on the build machine the legacy defaults key was empty, so live migration-with-data was not observed locally — it is proven by the migration test and will run on the first launch wherever real pre-Phase-8 data exists.
- Phase 9A machine validation (2026-06-11): Xcode-toolchain build succeeded; Xcode-toolchain test run passed **69 tests / 3 suites**; `./script/build_and_run.sh --verify` succeeded; executable and CodeResources existed; `codesign --verify --deep --strict dist/LocalForge.app` passed; LocalForge process stayed running; no LocalForge crash report observed; recent unified log sample showed no LocalForge error/fault/fatal lines; `workspace.sqlite` existed and had `SQLite format 3` header. Human runtime validation remains open.
- Phase 9B implementation validation (2026-06-11): Xcode-toolchain `swift build --cache-path .build/swiftpm-cache` succeeded after qualifying SwiftUI's `TimelineView`; Xcode-toolchain `swift test --cache-path .build/swiftpm-cache` passed **72 tests / 3 suites**. Added tests cover environment snapshot comparison, diagnostic background preference defaults from older JSON, test outcome release impact including Blocked, and full SQLite round-trip now includes environment snapshots + test records. `./script/build_and_run.sh --verify` exited 0 and re-signed `dist/LocalForge.app`; `codesign --verify --deep --strict dist/LocalForge.app` passed. Human UI click-through for the new registry/background flows remains open.
- Focused validation attempt (2026-06-11): `./script/build_and_run.sh --logs` surfaced one SwiftUI duplicate-ID runtime fault in Guardian latest activity; fixed by using enumerated offsets for repeated strings. Rebuild and `swift test` passed afterward (**72 tests / 3 suites**). A second `--logs` sample no longer showed that fault. Native UI click-through remains blocked for Codex because System Events lacks assistive access.
- Release process blocker (2026-06-11): `/Users/studiomacmini/Desktop/App assets/Dev App` is not a Git repository (`.git` missing; `git status --short` fails). This must be restored, initialized, or explicitly accepted/documented as a non-Git source bundle before release-quality work continues.
- Phase 9C professional polish (2026-06-11): Xcode-toolchain `swift build --cache-path .build/swiftpm-cache` passed; Xcode-toolchain `swift test --cache-path .build/swiftpm-cache` passed **73 tests / 3 suites**. Added diagnostic background density/motion persistence coverage. UI changes include fixed-grid background controls, CLI Companion polish, Test/Environment Registry boundary copy, and Command Centre build-recorded-but-unverified nudge. `./script/build_and_run.sh --verify` passed, `codesign --verify --deep --strict dist/LocalForge.app` passed, and the running app was confirmed from the current bundle at PID 74689.
- Phase 9D validation pass (2026-06-11): Git investigation confirmed `/Users/studiomacmini/Desktop/App assets/Dev App` contains the LocalForge SwiftPM package (`Package.swift`, `Sources`, `Tests`, `Docs`, `script`, `dist`) but is not under Git; `.git` is missing at this path and no parent Git root exists. Nearby sibling projects do have `.git`, so this is a non-repo LocalForge workspace/source bundle unless version control is restored or initialized explicitly. Xcode-toolchain `swift build --cache-path .build/swiftpm-cache` passed after rerunning outside the Codex sandbox due the known SwiftPM `sandbox-exec` manifest failure; Xcode-toolchain `swift test --cache-path .build/swiftpm-cache` passed **73 tests / 3 suites**; `./script/build_and_run.sh --verify` passed; `codesign --verify --deep --strict dist/LocalForge.app` passed; process list confirmed the current bundle running from `dist/LocalForge.app` at PID 86139, then logged launch confirmed current bundle path at PID 87452. No Phase 9D code defect was found; native click-through for background settings, Test Registry, Environment Registry, Utility Centre, and Backup import/export remains human-verifiable.
- Phase 9E remaining-work/deferred-path pass (2026-06-11): added non-mutating notarisation prerequisite helper `script/notarize.sh --check` and explicit `--submit` distribution mode requiring Developer ID identity + notarytool profile. Updated release/deferred docs to distinguish existing manual Build History -> Evidence, read-only Git scan, and selected-target large-file review from still-deferred automated build runner, Repo Monitor, whole-disk scanner, daemon, AI, automatic fixes, and automatic repo modification. `script/notarize.sh --check` passed against `dist/LocalForge.app`; Xcode-toolchain `swift build --cache-path .build/swiftpm-cache` passed after the known sandbox-manifest rerun; Xcode-toolchain `swift test --cache-path .build/swiftpm-cache` passed **73 tests / 3 suites**; `./script/build_and_run.sh --verify` passed; `codesign --verify --deep --strict dist/LocalForge.app` passed; process list confirmed current bundle running at PID 94848.
- Phase 10A Dev Tools implementation (2026-06-11): added preset-only Dev Tools module, `DevToolsCommand` / `DevToolsCommandResult`, and `DevCommandEngine` allowlist/blocklist enforcement. Presets are Swift Build, Swift Test, Git Status, Codesign Verify, Gatekeeper Check, and Environment Capture. Output feeds existing BuildRecord/TestRecord/EvidenceRecord/EnvironmentSnapshot paths; no free-form shell, automatic fixes, repo mutation, background polling, or whole-disk scanning was added. Xcode-toolchain `swift build --cache-path .build/swiftpm-cache` passed cleanly after the known sandbox-manifest rerun; `./script/build_and_run.sh --verify` passed; `codesign --verify --deep --strict dist/LocalForge.app` passed. New tests were added for allowed presets and blocked commands, but `swift test` could not be rerun in this pass because the external approval/usage reviewer rejected the required sandbox escape after the manifest-planning failure.
- Phase 10B professionalisation pass (2026-06-11): added shared explanation guidance to major UI modules and documented the V1.5 Development Intelligence roadmap. No new engines, telemetry, cloud services, AI execution, background polling, destructive automation, or automatic repository modification were added. Xcode-toolchain `swift build --cache-path .build/swiftpm-cache` passed after the known sandbox-manifest rerun; Xcode-toolchain `swift test --cache-path .build/swiftpm-cache` passed **77 tests / 4 suites**; `./script/build_and_run.sh --verify` passed; `codesign --verify --deep --strict dist/LocalForge.app` passed; process check confirmed the current rebuilt bundle running at PID 26091.
- Phase 10C / V1.6 safe intelligence pass (2026-06-11): added Recommendations module, per-project `RecommendationRecord`, approval-state metadata, and repo-scoped `CodeBloatScannerEngine` for source files over **1,750 lines**. Added tests for threshold flagging, excluded folders, non-mutating recommendation generation, backward-compatible recommendation decode, and explicit approval state transitions. Xcode-toolchain `swift build --cache-path .build/swiftpm-cache` passed after the known sandbox-manifest rerun; Xcode-toolchain `swift test --cache-path .build/swiftpm-cache` passed **82 tests / 5 suites**; `./script/build_and_run.sh --verify` passed; `codesign --verify --deep --strict dist/LocalForge.app` passed; process check confirmed the current rebuilt bundle running at PID 38210.
- Phase 10D Git state repair (2026-06-11): checked for `.git` in the LocalForge folder and immediate children; none existed. Nearby sibling projects had their own independent repos. With operator approval, initialised a fresh Git repository, added conservative `.gitignore`, and established the committed `main` baseline now visible as four commits: repository baseline, core engines, app command centre UI, and core coverage.
- Phase 10E roadmap/release baseline (2026-06-12): confirmed `git status -sb` clean on `main`; latest commit `89257a0 test: add LocalForge core coverage`; Xcode-toolchain `swift build --cache-path .build/swiftpm-cache` passed; Xcode-toolchain `swift test --cache-path .build/swiftpm-cache` passed with **82 tests / 5 suites**; `./script/build_and_run.sh --verify` passed; `codesign --verify --deep --strict --verbose=2 dist/LocalForge.app` passed. `codesign -dv --verbose=4 dist/LocalForge.app` confirmed the local development bundle is ad-hoc signed, not Developer ID signed. Human Phase 10F validation remains open.

Verification chain still needed for future releases:

Implemented -> Built -> Loaded -> Visible -> Reachable -> Functional -> Tested -> Observed -> Verified.
